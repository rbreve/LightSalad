<h1>Editing list</h1>

<%= error_messages_for :list %>

<% form_for(@list) do |f| %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @list %> |
<%= link_to 'Back', lists_path %>
